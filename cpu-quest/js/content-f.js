/* ============ 世界9：一机多程（操作系统） ============ */
LEVELS["w9a"] = {
  icon:"🎩", title:"进程：让一个CPU骗过所有人", xp:130,
  hook:"你开着几十个程序,CPU核却没几个",
  blocks:[
    {t:"story", h:"📖 一个数不过来的账", html:`
      <p>看看你现在的电脑:浏览器、音乐、微信、编辑器……几十上百个程序在「同时」运行。</p>
      <p>可你的 CPU 也许只有 8 个核。<b>8 个核,怎么同时跑几百个程序?</b></p>
      <p>答案有点狡猾:<b>它根本没有真的同时跑。它在飞快地轮流,快到你的眼睛和感觉完全被骗过去。</b></p>
      <p>操作系统就是那个总导演,它变的这套「人人都以为自己独占电脑」的戏法,叫<b>进程</b>。
      这一关拆穿它。</p>`},

    {t:"text", h:"📦 进程:一个程序的「运行实例」", html:`
      <p>先分清两个词(无数人搞混):</p>
      <blockquote>
        <b>程序(program):</b>躺在硬盘上的一个文件,一堆指令,是死的。(像菜谱)<br>
        <b>进程(process):</b>这个程序被<u>装进内存、跑起来</u>后的实例,是活的。(像正在按菜谱做菜的这一次)
      </blockquote>
      <p>同一个程序可以开出多个进程 —— 你开两个记事本窗口,就是一个程序、两个进程,各有各的内存。</p>
      <p>操作系统给每个进程发一整套「家当」,让它以为自己独占了整台机器:</p>
      <ul>
        <li><b>独立的地址空间</b>(它以为整块内存都是自己的 —— 这是下一关的虚拟内存)</li>
        <li><b>一套寄存器状态</b>(它自己的 PC、栈指针等)</li>
        <li><b>打开的文件、网络连接</b>等资源清单</li>
      </ul>`},

    {t:"lab", h:"⚡ 实验室:单核如何「同时」跑四个程序", lab:"sched",
     html:`<p>点「开始调度」,盯住上方「CPU现在在跑谁」:</p>
     <ul>
       <li>任一瞬间,<b>CPU只在跑一个进程</b>,其他三个被冻在原地干等。</li>
       <li>但它切换得飞快,四个进度条<b>看起来都在往前爬</b> —— 这就是你感受到的「同时」。</li>
     </ul>
     <p>真实系统里每个时间片约几毫秒,一秒切换成百上千次。<b>你以为的并行,其实是极快的串行轮换。</b>
     (真正的并行需要多核 —— 那才是几个核真的在同一时刻各跑各的。)</p>`},

    {t:"aha", h:"💡 上下文切换:戏法的成本", html:`
      <p>CPU 从进程A切到进程B,不是说切就切。A 的整个「现场」得先保存下来,B 的现场得恢复回去 ——
      这个动作叫<b>上下文切换(context switch)</b>:</p>
      <ol>
        <li>把 A 的所有寄存器(PC、栈指针、通用寄存器…)存进内存</li>
        <li>切换地址空间(换页表 —— 下一关细讲)</li>
        <li>把 B 之前存的寄存器全部恢复</li>
        <li>跳到 B 上次被打断的地方继续</li>
      </ol>
      <p><b>这有实实在在的成本</b>(几微秒,外加切换后缓存全是冷的、要重新预热)。所以:</p>
      <ul>
        <li>切太<b>勤</b>:响应快,但一堆时间浪费在切换本身,总吞吐下降。</li>
        <li>切太<b>懒</b>:吞吐高,但某个进程可能卡很久没人管,你会觉得电脑「卡」。</li>
      </ul>
      <p>操作系统的<b>调度器</b>就是在这两者间走钢丝,还要兼顾公平和优先级。
      「时间片」多长、谁先跑,是操作系统里一门大学问。</p>`},

    {t:"text", h:"🧵 线程:进程内部的「分身」", html:`
      <p>进程之间是互相隔离的(各有各的内存,一个崩了不连累别人),这很安全,但<b>它们之间通信、切换都贵</b>。</p>
      <p>于是有了更轻的东西 —— <b>线程(thread)</b>:同一个进程内的多个执行流。</p>
      <table>
        <tr><th></th><th>多进程</th><th>多线程</th></tr>
        <tr><td>内存</td><td>各自独立(隔离)</td><td><b>共享同一块</b></td></tr>
        <tr><td>通信</td><td>贵(要专门的IPC机制)</td><td>便宜(直接读写共享变量)</td></tr>
        <tr><td>一个崩了</td><td>不影响其他进程</td><td>整个进程一起挂</td></tr>
        <tr><td>切换成本</td><td>高(要换地址空间)</td><td>低(共享地址空间)</td></tr>
      </table>
      <p>比喻:进程是各自独立的公司(有自己的办公楼),线程是一个公司里的多个员工(共用办公室和文件柜)。
      员工沟通快(共享),但一个人打翻咖啡泼了公共文件柜,大家都遭殃。</p>
      <p><b>共享内存是双刃剑</b>:两个线程同时改一个变量,就出<b>竞态条件(race condition)</b> ——
      结果取决于谁先谁后,时好时坏,是最难查的一类 bug。锁、原子操作就是为了驯服它,那是并发编程的大门。</p>`},

    {t:"warn", h:"⚠️ 内核态 vs 用户态:一道护城河", html:`
      <p>如果每个程序都能直接操作硬件、访问任意内存,一个流氓程序就能搞垮整台机器。所以 CPU 硬件层面分了两个特权级:</p>
      <ul>
        <li><b>用户态(user mode):</b>你的程序平时待的地方。权限受限,不能直接碰硬件、不能访问别人的内存。</li>
        <li><b>内核态(kernel mode):</b>操作系统内核待的地方。全部权限,能操作一切硬件。</li>
      </ul>
      <p>那你的程序要读文件、发网络、申请内存怎么办?<b>得向操作系统「打报告」——这叫系统调用(syscall)</b>。
      程序执行一条特殊指令,CPU 从用户态<b>陷入(trap)</b>内核态,由操作系统代劳,干完再切回来。</p>
      <p>这解释了几件事:</p>
      <ul>
        <li>为什么频繁的小文件读写慢 —— 每次 syscall 都要陷入内核、切换特权级,有固定开销(所以要用缓冲/批量)。</li>
        <li>为什么 <code>printf</code> 比想象的重 —— 它最终是个 write 系统调用。</li>
        <li>为什么普通程序删不掉系统文件、装不了驱动 —— 权限被用户态挡住了。</li>
      </ul>
      <blockquote>🎯 这就是世界5说的「系统/特殊指令」那一类的真面目:它们是<b>用户态程序向内核请求服务的敲门砖</b>。</blockquote>`}
  ],
  quiz:[
    {id:"w9aq1", q:"「程序」和「进程」的区别是?",
     opts:["没区别","程序是硬盘上静态的指令文件,进程是程序被加载运行起来的动态实例","进程更小","程序运行更快"],
     answer:1, ex:"程序=硬盘上死的文件(像菜谱)，进程=它跑起来的活实例(像正在照菜谱做的这一次)。同一程序可开多个进程，各有独立内存。"},
    {id:"w9aq2", q:"单核 CPU 如何「同时」运行多个程序?",
     opts:["真的同时执行","飞快地在进程间轮流切换,快到人感觉不到,是极快的串行","把程序合并成一个","用了多个缓存"],
     answer:1, ex:"分时复用：每个进程跑一个几毫秒的时间片就被切走，一秒切换成百上千次，制造「同时」的错觉。真并行需要多核(多个核同一时刻各跑各的)。"},
    {id:"w9aq3", q:"「上下文切换」为什么有成本?",
     opts:["需要重启程序","要保存旧进程的全部寄存器状态、切换地址空间、恢复新进程状态,且切换后缓存变冷","会丢失数据","需要联网"],
     answer:1, ex:"保存/恢复寄存器、切换页表都要时间(几微秒)，切换后新进程的缓存是冷的还要重新预热。切太勤会让开销吃掉吞吐，调度器要在响应和吞吐间权衡。"},
    {id:"w9aq4", q:"多线程相比多进程,最主要的特点是?",
     opts:["线程更安全","同进程的线程共享内存,通信快、切换轻,但一个线程崩溃会拖垮整个进程","线程不能并行","线程更慢"],
     answer:1, ex:"线程共享进程的地址空间：通信便宜、切换轻。代价是隔离性差(一个线程搞崩，整个进程挂)，且共享内存带来竞态条件这类最难查的并发bug。"},
    {id:"w9aq5", q:"程序想读文件、发网络请求,为什么必须通过「系统调用」?",
     opts:["为了记录日志","普通程序在受限的用户态,无权直接操作硬件,只能陷入内核态请求操作系统代劳","系统调用更快","这是编程语言的规定"],
     answer:1, ex:"用户态权限受限(不能直接碰硬件/别人的内存)，这是保护机制。要用硬件资源就得通过syscall陷入内核态由OS代劳。这也是频繁小IO慢、需要缓冲批量的原因。"}
  ]
};

LEVELS["w9b"] = {
  icon:"🃏", title:"虚拟内存:最大的那个骗术", xp:140,
  hook:"每个程序都以为自己独占整块内存",
  blocks:[
    {t:"story", h:"📖 一个不可能的承诺", html:`
      <p>上一关说,每个进程都以为自己独占整台机器的内存。可是:</p>
      <ul>
        <li>你有几十个进程,它们的地址会不会撞车?(程序A用地址1000,程序B也用地址1000)</li>
        <li>一个流氓进程能不能偷看/篡改另一个进程的内存?</li>
        <li>物理内存只有16GB,可每个64位程序都以为自己有海量地址空间 —— 怎么兑现?</li>
      </ul>
      <p>操作系统 + 一块叫 <b>MMU</b> 的硬件,用一个天才的骗术同时解决了这三个问题。
      这可能是整个计算机体系里最优雅的一层抽象:<b>虚拟内存</b>。</p>`},

    {t:"text", h:"🎭 核心戏法:地址是假的", html:`
      <p>戏法的核心一句话:<b>程序看到和使用的所有地址,全是「假地址」(虚拟地址)。
      真正的物理内存地址,程序永远看不到。</b></p>
      <p>程序说「我要访问地址9000」,这个9000是虚拟的。CPU里的 <b>MMU(内存管理单元)</b>硬件
      拦下这个请求,查一张<b>页表(page table)</b>,把9000<b>翻译</b>成真实的物理地址,再去访问。</p>
      <p>为了不用给每个字节都记一条翻译,内存被切成固定大小的块 —— <b>页(page)</b>,通常4KB。
      翻译以页为单位:</p>
      <pre><code>虚拟地址 9000
  ÷ 4096  →  第 2 页,页内偏移 808
  查页表  →  第 2 虚拟页 映射到 第 9 物理页框
  真实地址 = 9 × 4096 + 808 = 37672</code></pre>
      <p>每个进程有<b>自己的一张页表</b>。所以进程A的「虚拟页2」和进程B的「虚拟页2」
      被映射到<b>完全不同的物理位置</b> —— 地址再也不会撞车了。</p>`},

    {t:"lab", h:"⚡ 实验室:地址翻译机", lab:"vm",
     html:`<p>输入一个虚拟地址,看 MMU 一步步把它翻译成物理地址(右边是页表)。<b>务必试这三个:</b></p>
     <ol>
       <li><b>9000</b>:正常翻译,看它怎么拆成「页号+偏移」再查表。</li>
       <li><b>13000</b>(落在虚拟页3):页表里它「无效」——<b>这就是缺页,OS要去硬盘搬。</b></li>
       <li><b>21000</b>(页表里根本没这一页):<b>Segmentation fault</b> —— 世界7那个段错误,根源就在这里!</li>
     </ol></p>`},

    {t:"aha", h:"💡 一个骗术,四个红利", html:`
      <p>看懂了地址翻译,你会发现它顺手解决了一大堆问题:</p>
      <ol>
        <li><b>隔离与安全:</b>进程只能访问自己页表里有的页。想碰别人的内存?页表里根本没这一项,
        MMU直接拦下 → 段错误。<b>世界7的Segmentation fault,底层就是MMU在这里报警。</b></li>
        <li><b>不用连续的物理内存:</b>程序以为自己的内存是一整块连续的,实际上物理上可以东一页西一页碎着放,
        页表负责「拼接」的假象。内存碎片问题迎刃而解。</li>
        <li><b>内存可以「超卖」:</b>物理内存不够时,把暂时不用的页偷偷挪到硬盘上(swap/换页),
        用到时再搬回来。所以你能开超过物理内存总量的程序 —— 代价是频繁换页会让电脑狂卡(thrashing)。</li>
        <li><b>共享与省内存:</b>多个进程跑同一个程序,代码段只需在物理内存留一份,
        大家的页表都指向它。<code>fork</code> 时的「写时复制(copy-on-write)」也靠这个 —— 先共享,谁改了才真正复制。</li>
      </ol>
      <p>这就是抽象的威力:<b>加一层「假地址→真地址」的翻译,竟一口气买到了隔离、安全、灵活布局、超卖、共享五样东西。</b></p>`},

    {t:"text", h:"⏱️ 天下没有免费的翻译:TLB", html:`
      <p>等等 —— <b>每次访问内存,都要先查一次页表?而页表本身也在内存里?</b>
      那岂不是每次访存变成两次(先查表、再访问)?更糟的是现代页表是多级的,查一次要访存好几遍。</p>
      <p>这个矛盾,又被「缓存」思想解决了(看,世界8的思路无处不在)。CPU里有个专门缓存翻译结果的小硬件 ——
      <b>TLB(translation lookaside buffer)</b>,存最近用过的「虚拟页→物理页框」对应关系。</p>
      <ul>
        <li>大多数访问,翻译结果就在TLB里(命中),几乎零成本。</li>
        <li>偶尔TLB没有(未命中),才老实去查页表,查完再存进TLB。</li>
      </ul>
      <p>还记得上一关的上下文切换很贵吗?一部分原因就在这:<b>切换进程要换页表,TLB里缓存的旧翻译全作废,
      得重新预热。</b>这就是为什么进程切换比线程切换贵。</p>
      <blockquote>🎯 至此你集齐了三层「缓存」思想:<b>CPU缓存</b>挡住慢内存、<b>TLB</b>挡住慢页表查询、
      内存本身又<b>挡住</b>更慢的硬盘。<b>「用一个快的小东西挡在慢的大东西前面」——
      这个模式,是整个计算机系统性能的第一性原理。</b></blockquote>`}
  ],
  quiz:[
    {id:"w9bq1", q:"虚拟内存最核心的机制是什么?",
     opts:["把内存压缩","程序使用虚拟地址,由MMU硬件查页表翻译成物理地址,程序永远看不到真实物理地址","把内存搬到硬盘","增加内存容量"],
     answer:1, ex:"核心=地址翻译。程序用的全是虚拟地址，MMU查页表翻译成物理地址。每个进程有独立页表，所以虚拟地址相同也会映射到不同物理位置，互不干扰。"},
    {id:"w9bq2", q:"为什么两个进程可以用相同的虚拟地址而不冲突?",
     opts:["操作系统禁止相同地址","每个进程有独立的页表,同一虚拟地址被翻译到不同的物理地址","地址会自动错开","它们分时使用"],
     answer:1, ex:"每个进程一张自己的页表。进程A的虚拟页2和进程B的虚拟页2，经各自页表映射到完全不同的物理页框，物理上根本不是同一块，自然不冲突。"},
    {id:"w9bq3", q:"世界7学的「Segmentation fault」,用虚拟内存的原理解释是?",
     opts:["内存条坏了","程序访问的虚拟地址在页表里没有有效映射(或权限不符),MMU拦截并报错","页表满了","地址翻译太慢"],
     answer:1, ex:"访问的虚拟页在页表中不存在或权限不对(比如写只读页)，MMU当场发现越权，触发异常，OS终止进程。段错误的底层就是MMU在页表这一层的保护动作。"},
    {id:"w9bq4", q:"虚拟内存能让你运行「超过物理内存大小」的程序,靠的是?",
     opts:["压缩数据","把暂时不用的页换出到硬盘(swap),需要时再换回,物理内存像被「超卖」","借用其他进程内存","禁用部分功能"],
     answer:1, ex:"页可以在物理内存和硬盘间换进换出。不常用的页挪到硬盘，腾出物理内存。代价是换页频繁时(thrashing)性能暴跌，因为硬盘比内存慢好几个数量级。"},
    {id:"w9bq5", q:"TLB 的作用是什么?",
     opts:["扩大内存容量","缓存最近的地址翻译结果,避免每次访存都去查页表(否则一次访问变多次)","加速硬盘","管理进程调度"],
     answer:1, ex:"TLB是「地址翻译的缓存」。没它的话每次访存都要先查(多级)页表，代价巨大。它体现了和CPU缓存同一个思想。进程切换要换页表使TLB失效，是上下文切换昂贵的原因之一。"}
  ]
};

WORLDS.push({id:"w9", num:"世界 9", icon:"🏰", name:"一机多程:操作系统", sub:"进程、线程与虚拟内存的骗术", nodes:["w9a","w9b"]});

/* ============ 终章 ============ */
LEVELS["end"] = {
  icon:"👑", title:"全景回望:a+b 的一生", xp:200,
  hook:"把八层楼一次性串起来,然后毕业",
  blocks:[
    {t:"story", h:"📖 你已经爬完了整座塔", html:`
      <p>还记得序章那张塔吗?从沙子到程序,八层楼。<b>现在你每一层都造过、玩过、答过题了。</b></p>
      <p>这一关不教新东西。我们做一件更酷的事:拿一行最普通的代码,<b>让它从最上层一路掉到最底层,
      看看沿途每一层楼都做了什么。</b>如果你能跟下来,你就真的贯通了。</p>`},

    {t:"lab", h:"🗺️ 回顾:你爬过的塔", lab:"tower",
     html:`<p>再看一眼这张图。这次,每一层你都应该能说出「它在干什么、我在哪一关玩过它」。</p>`},

    {t:"text", h:"🎬 一行代码的一生:c = a + b;", html:`
      <p>假设 <code>a</code>、<code>b</code>、<code>c</code> 是三个局部整型变量。看这行代码穿越八层楼:</p>
      <pre><code>【第8层·你的代码】     c = a + b;

【第7层·编译器】       编译成机器指令(世界6):
                       mov  eax, [rbp-4]   ; 从栈里取 a
                       add  eax, [rbp-8]   ; 加上 b
                       mov  [rbp-12], eax  ; 存回 c
                       ↑ a,b,c 是局部变量,住在【栈】上(世界7)

【第6层·操作系统】     进程被调度到某个CPU核上运行(世界9)
                       [rbp-4] 这些是【虚拟地址】,要经MMU翻译(世界9)

【第5层·指令集ISA】    mov/add 是这颗CPU认识的指令(世界5)
                       CPU开始它永恒的循环:取指→译码→执行(世界4)

【第4层·微架构】       取 a: MMU查TLB翻译地址 → 查L1缓存
                       命中!(世界8) a 的值进入寄存器
                       add 指令进入流水线的EX阶段(世界8)

【第3层·数字逻辑】     add 落到【ALU】上执行(世界2、4)
                       ALU内部是一排【全加器】(世界2)

【第2层·门电路】       每个全加器 = 2个XOR + 2个AND + 1个OR(世界2)
                       进位一位一位地波动传递

【第1层·比特与晶体管】 每个门 = 几个【晶体管】开关(世界1、2)
                       a、b 是【补码】表示的比特串(世界3)
                       高电压=1,低电压=0(世界1)

【结果往回冒】         和的比特串 → 寄存器 → 经缓存 → 写回栈上的 c
                       整个过程:约几个纳秒。</code></pre>
      <p><b>就这样。</b>你写的 <code>a + b</code> 三个字符,背后是八层楼、几十亿个晶体管、
      一整套操作系统的骗术,在几纳秒内协同完成的一次接力。<b>而现在,每一棒你都认识。</b></p>`},

    {t:"aha", h:"💡 如果只带走三句话", html:`
      <p>整个旅程,浓缩成三句能用一辈子的话:</p>
      <blockquote>
        <b>1. 一切都是抽象的堆叠。</b><br>
        用简单的造复杂的,包起来,再往上造。看不懂一个系统时,先问「它建在哪一层、下面那层给了它什么」。<br><br>
        <b>2. CPU一辈子只做一件事:取指→译码→执行,重复几十亿次。</b><br>
        再花哨的软件(操作系统、浏览器、AI),归根结底都是这个循环在跑不同的指令。没有魔法。<br><br>
        <b>3. 性能的第一性原理:用快的小东西,挡在慢的大东西前面。</b><br>
        寄存器挡内存、缓存挡内存、TLB挡页表、内存挡硬盘。看到性能问题,先想「谁在等谁,能不能加个缓存」。
      </blockquote>`},

    {t:"text", h:"🧭 接下来去哪:给你的路标", html:`
      <p>这座塔给了你<b>整个框架和直觉</b>。现在你可以带着地图,去任何一层深挖而不迷路了:</p>
      <ul>
        <li><b>想深入CPU/体系结构</b> → 现在再读 CSAPP(《深入理解计算机系统》),
        你会发现它不再枯燥 —— 因为你已经知道每一章在塔的哪一层。它第2章(数据)、3章(汇编)、
        6章(存储/缓存)你已经有直觉了,读起来是「印证」而非「啃生肉」。</li>
        <li><b>想动手造CPU</b> → Nand2Tetris(《计算机系统要素》),真的从NAND门搭到能跑俄罗斯方块的电脑。</li>
        <li><b>想深入操作系统</b> → 《操作系统导论》(OSTEP),免费,讲进程/内存/并发极其清楚。</li>
        <li><b>想验证本站学的</b> → 打开终端,<code>gcc -S test.c</code> 看汇编,
        <code>htop</code> 看进程调度,<code>valgrind</code> 抓内存bug。你现在看得懂它们在说什么了。</li>
      </ul>
      <blockquote>🎓 <b>枯燥的根源是「不知道自己在往哪走」。现在你有地图了。</b>
      去把每一层挖深吧 —— 但你永远不会再迷路。</blockquote>`}
  ],
  quiz:[
    {id:"endq1", q:"贯穿全站的「抽象」思想,一句话概括是?",
     opts:["让代码更简洁","用简单的部件搭出复杂的,封装成新部件再往上搭,每层只需关心下层的接口","减少bug","提高运行速度"],
     answer:1, ex:"抽象=分层封装。晶体管→门→ALU→CPU→指令→OS→你的代码，每一层把下层包成「黑盒」，只暴露接口。这让人类能协作造出十亿行级的系统。"},
    {id:"endq2", q:"无论多复杂的软件,CPU执行它的本质都是?",
     opts:["调用操作系统","不断重复:取指→译码→执行","编译成机器码","分配内存"],
     answer:1, ex:"取指-译码-执行的无限循环，每秒几十亿次。操作系统、浏览器、AI大模型，最终都是这个循环在跑海量指令而已，没有别的魔法。这是本站最重要的一句话。"},
    {id:"endq3", q:"寄存器、CPU缓存、TLB、内存(相对硬盘),它们体现的是同一个思想:",
     opts:["都用来存数据","用一个更快但更小的存储,挡在更慢更大的存储前面,靠局部性提升平均速度","都在CPU里","都会断电丢失"],
     answer:1, ex:"这是性能的第一性原理。缓存挡内存、TLB挡页表、内存挡硬盘——层层都是「快的小东西挡在慢的大东西前」。遇到性能问题先想「谁在等，能否加缓存」。"},
    {id:"endq4", q:"c = a + b 这行代码执行时,a 和 b 作为局部变量,它们的比特串在CPU里做加法用的是?",
     opts:["专门的加法芯片","ALU,其内部由全加器阵列构成,而全加器由基本逻辑门搭成","操作系统的加法函数","查一张加法表"],
     answer:1, ex:"add指令落到ALU执行，ALU的加法部分就是一排全加器(世界2造的)，每个全加器=几个XOR/AND/OR门=一堆晶体管。你写的+号，一路通到了沙子。"},
    {id:"endq5", q:"学完这座塔,面对「感觉枯燥/学不下去」的正确心法是?",
     opts:["死记硬背每个细节","先有整体框架和地图,再带着「我在哪一层」的定位去深挖细节,就不会迷路","跳过难的部分","只学最上层"],
     answer:1, ex:"枯燥的根源是不知道自己在往哪走。有了全局地图，再深入任何一层(读CSAPP、造CPU、学OS)都是「印证和填充」，而不是啃生肉。框架先行，细节随后。"}
  ]
};

WORLDS.push({id:"wend", num:"终章", icon:"👑", name:"全景回望", sub:"串起八层楼,然后毕业", nodes:["end"]});

/* ============ 世界8：让它更快（缓存 / 流水线） ============ */
LEVELS["w8a"] = {
  icon:"⚡", title:"缓存：CPU 的近身口袋", xp:120,
  hook:"同样的代码，换个循环顺序快 5 倍",
  blocks:[
    {t:"story", h:"📖 一个尴尬的真相", html:`
      <p>你以为 CPU 最大的敌人是「算得慢」。错。<b>CPU 大部分时间在<u>等</u>。</b></p>
      <p>回忆世界4:一个 CPU 周期约 0.3 纳秒。而从内存条读一个数,要 <b>100 纳秒以上</b> ——
      相当于 CPU 干等 <b>300 多个周期</b>。用世界9你会看到的比喻:如果一个周期是 1 秒,
      那访问一次内存就是干坐 5 分钟。</p>
      <p>如果每条指令都要去内存拿数据,再快的 CPU 也会被活活拖死。
      <b>整个现代计算机性能的一半故事,都是围绕「怎么少去内存」展开的。</b>
      解法就是这一关的主角:缓存。</p>`},

    {t:"text", h:"🏔️ 存储金字塔:又快又大是幻觉", html:`
      <p>世界1提过「越快越贵越小」。工程师用一个绝招把这个矛盾藏了起来 ——
      <b>搭一座金字塔,让快的挡在慢的前面</b>:</p>
      <table>
        <tr><th>层级</th><th>访问耗时</th><th>容量</th><th>类比(周期=1秒)</th></tr>
        <tr><td>寄存器</td><td>&lt;1 周期</td><td>~1KB</td><td>就在手上</td></tr>
        <tr><td>L1 缓存</td><td>~4 周期</td><td>~32KB</td><td>桌上,伸手可拿</td></tr>
        <tr><td>L2 缓存</td><td>~12 周期</td><td>~256KB-1MB</td><td>走到书架</td></tr>
        <tr><td>L3 缓存</td><td>~40 周期</td><td>几十MB</td><td>下楼取件</td></tr>
        <tr><td>内存 DRAM</td><td>~300 周期</td><td>几十GB</td><td>开车去仓库(5分钟)</td></tr>
        <tr><td>SSD</td><td>~10万+ 周期</td><td>几TB</td><td>坐飞机取货(几天)</td></tr>
      </table>
      <p>关键点:<b>这些层是硬件自动管理的,你的代码看不见它们</b>(不像堆栈要你操心)。
      CPU 要一个数据时,先问 L1「有吗」,没有问 L2,再没有问 L3,最后才惊动内存。
      找到了叫<b>命中(hit)</b>,没找到叫<b>未命中(miss)</b>。</p>`},

    {t:"aha", h:"💡 缓存能work,全靠两条「局部性」", html:`
      <p>缓存这么小(L1才32KB),凭什么能挡住大部分内存访问?靠一个关于程序行为的经验规律 ——
      <b>局部性原理</b>:</p>
      <blockquote>
        <b>时间局部性:</b>刚用过的数据,很可能马上又要用。<br>
        (循环变量 <code>i</code>、刚读的对象,一会儿还会碰)<br><br>
        <b>空间局部性:</b>用了某个数据,很可能马上要用它旁边的。<br>
        (数组挨个遍历、结构体的字段一个接一个访问)
      </blockquote>
      <p>基于空间局部性,缓存有个重要设计:<b>它不按「一个字节」搬,而是按「一整块」搬</b>
      (通常 64 字节,叫<b>缓存行 cache line</b>)。你只要了 <code>a[0]</code>,
      硬件顺手把 <code>a[0]~a[15]</code> 一整行都拉进缓存。所以你接着访问 <code>a[1]</code> 时,
      <b>它已经在缓存里等着了 —— 免费的命中。</b></p>
      <p>这一条,直接决定了下面那个「快5倍」的实验。</p>`},

    {t:"lab", h:"⚡ 实验室:两种遍历顺序的对决", lab:"cache",
     html:`<p>同一个 8×8 二维数组,同样访问全部 64 个元素,只是顺序不同。<b>请两种都点一遍「开始访问」,对比命中率:</b></p>
     <ul>
       <li><b>按行 a[i][j]</b>:顺着内存存放的方向走。搬进来一整行,接下来几个访问全命中。</li>
       <li><b>按列 a[j][i]</b>:每次跳到很远的地方,把刚搬进来的行浪费掉,几乎次次未命中。</li>
     </ul>
     <p>看右侧「估算耗时」的差距 —— <b>算法复杂度完全一样(都是O(n²)),就因为访问顺序,性能差好几倍。</b>
     这是缓存给你上的第一课。</p>`},

    {t:"text", h:"🛠️ 缓存意识:能立刻用上的几条", html:`
      <p><b>1. 数组遍历顺序要「顺着内存」。</b>C/C++/Python(NumPy) 是行优先存储,
      就该 <code>for i: for j: a[i][j]</code>。Fortran/MATLAB 是列优先,反过来。搞反了直接慢几倍。</p>
      <p><b>2. 数据结构紧凑排列。</b>数组(元素连续)对缓存极友好;链表(节点散落各处)对缓存很不友好 ——
      这就是为什么现代很多场景「数组 + 二分」实际比链表快,哪怕理论复杂度看着差不多。</p>
      <p><b>3. 结构体字段按访问模式组织。</b>经常一起用的字段放一起,能落进同一个缓存行。
      游戏引擎流行的「数据导向设计(DOD)」核心就是这个:把要一起遍历的数据排成连续数组。</p>
      <p><b>4. 小心「伪共享(false sharing)」。</b>多线程时,两个线程各改各的变量,
      但如果这俩变量恰好在同一个缓存行里,缓存一致性协议会让它们互相拖累,性能暴跌。</p>
      <blockquote>🎯 一句话:<b>CPU 喜欢你「一件接一件、挨着拿」,讨厌你「东一榔头西一棒子」。</b>
      写代码时脑子里有这张缓存图,你就已经领先大多数人了。</blockquote>`},

    {t:"lab", h:"⚡ 实验室:把纳秒放大成人能懂的时间", lab:"latency",
     html:`<p>纳秒太抽象。这张表把「一个CPU周期」放大成「1秒」,其余等待就变成你能体会的时间尺度。
     <b>扫一眼最后两行你就懂了为什么工程师那么怕碰硬盘和网络。</b>这张表是所有性能直觉的源头,建议存进脑子。</p>`}
  ],
  quiz:[
    {id:"w8aq1", q:"引入多级缓存(L1/L2/L3)最根本的原因是?",
     opts:["为了省电","CPU 和内存的速度差距巨大(相差百倍),需要快的小存储挡在前面","为了兼容老程序","内存容量不够"],
     answer:1, ex:"CPU 一个周期~0.3ns,内存访问~100ns+,差几百倍。缓存用「小而快」挡在「大而慢」的内存前面,靠局部性命中大部分请求,把平均访问时间拉下来。"},
    {id:"w8aq2", q:"缓存一次从内存搬运的单位是?",
     opts:["1 个字节","1 个字(4/8字节)","一整个缓存行(通常64字节)","一整个页(4KB)"],
     answer:2, ex:"缓存行(cache line),通常64字节。基于空间局部性:你要a[0]，硬件顺手把相邻的一整行搬来，接下来访问a[1]、a[2]就免费命中。"},
    {id:"w8aq3", q:"为什么二维数组「按行遍历」通常比「按列遍历」快很多?",
     opts:["按行代码更短","行优先存储下，同一行元素在内存中连续，符合空间局部性，缓存命中率高","按列会出错","编译器不支持按列"],
     answer:1, ex:"C系语言行优先：a[i][0],a[i][1]...在内存里连续。按行走顺着缓存行，一次搬入多次命中；按列走每次跳很远，反复未命中。复杂度相同但实测差几倍。"},
    {id:"w8aq4", q:"为什么数组通常比链表更「缓存友好」?",
     opts:["数组更小","数组元素在内存中连续存放，一个缓存行能装多个元素；链表节点散落各处","链表不能缓存","数组不用指针"],
     answer:1, ex:"数组连续，遍历时空间局部性极好。链表节点由malloc分散分配，遍历时每跳一个节点很可能就是一次cache miss。这让数组在实践中常常反超链表。"},
    {id:"w8aq5", q:"关于局部性原理，下面哪个是「时间局部性」的例子?",
     opts:["遍历数组的相邻元素","循环里反复使用同一个计数器变量 i","读取结构体的连续字段","按顺序读文件"],
     answer:1, ex:"时间局部性=刚用过的马上还会用（循环变量i每次迭代都碰）。其余三个是空间局部性=用了某数据，旁边的也快要用。缓存同时利用这两条规律。"}
  ]
};

LEVELS["w8b"] = {
  icon:"🏭", title:"流水线:让CPU同时干很多活", xp:120,
  hook:"洗衣店的智慧,和它的代价",
  blocks:[
    {t:"story", h:"📖 洗衣店老板的顿悟", html:`
      <p>你开洗衣店。一批衣服要经过三道工序:<b>洗(30分)→ 烘(30分)→ 叠(30分)</b>。</p>
      <p>笨办法:第一批洗完、烘完、叠完(90分钟),才开始洗第二批。三批要 270 分钟。</p>
      <p>聪明办法:<b>第一批在烘的时候,洗衣机空着干嘛?赶紧洗第二批!</b>
      于是洗衣机、烘干机、叠衣台<b>同时都在转</b>,只是处理着不同批次。三批只要 150 分钟。</p>
      <p>CPU 执行指令,也正好是几道固定工序。这个「让每个部件都别闲着」的思路,
      就是<b>流水线(pipeline)</b> —— 现代 CPU 提速的头号功臣。</p>`},

    {t:"text", h:"🔧 一条指令的五道工序", html:`
      <p>经典的 5 级流水线,把「取指-译码-执行」拆得更细:</p>
      <table>
        <tr><th>阶段</th><th>缩写</th><th>干什么</th><th>用到的部件</th></tr>
        <tr><td>取指</td><td>IF</td><td>从内存/缓存取指令</td><td>PC、指令缓存</td></tr>
        <tr><td>译码</td><td>ID</td><td>看懂指令、读寄存器</td><td>控制单元、寄存器组</td></tr>
        <tr><td>执行</td><td>EX</td><td>ALU 算数/算地址</td><td>ALU</td></tr>
        <tr><td>访存</td><td>ME</td><td>读写内存(如需要)</td><td>数据缓存</td></tr>
        <tr><td>写回</td><td>WB</td><td>结果写回寄存器</td><td>寄存器组</td></tr>
      </table>
      <p>关键洞察:<b>这五个阶段用的是<u>不同</u>的硬件部件</b>。所以当指令1在「执行(EX)」时,
      指令2完全可以同时在「译码(ID)」,指令3在「取指(IF)」—— 三个部件并行工作,谁都不闲着。</p>`},

    {t:"lab", h:"⚡ 实验室:流水线甘特图", lab:"pipeline",
     html:`<p>切换「用/不用流水线」,看那张时空表:</p>
     <ul>
       <li><b>不用</b>:每条指令老老实实走完5步,下一条才开始。5条指令要 25 个周期,每个部件80%时间在摸鱼。</li>
       <li><b>用</b>:错开一个周期就发射下一条,像瀑布一样。5条指令只要 9 个周期。</li>
     </ul>
     <p><b>特别注意那句提示:单条指令并没有变快(还是5步),变快的是「吞吐量」</b> ——
     单位时间流出的指令更多了。这个「延迟 vs 吞吐」的区别,是理解性能的一个重要分水岭。</p>`},

    {t:"aha", h:"💡 延迟 vs 吞吐:两个别搞混的概念", html:`
      <p>流水线揭示了性能里最容易被搞混的一对概念:</p>
      <blockquote>
        <b>延迟(latency):</b>一件事从开始到结束多久。<br>
        (一条指令走完5级 = 5个周期,流水线没让它变快)<br><br>
        <b>吞吐(throughput):</b>单位时间能完成多少件事。<br>
        (流水线满载后,理想情况每个周期「流出」1条指令 —— 提升5倍)
      </blockquote>
      <p>生活里到处是这个区别:<b>一个快递从北京到上海要1天(延迟),但顺丰每天能送出百万件(吞吐)。</b>
      加开分拣线不会让单个包裹更快到,但总处理量暴涨。</p>
      <p>工程决策常常要选边站:实时游戏要低<b>延迟</b>(单帧尽快出来),
      数据分析要高<b>吞吐</b>(总量尽快跑完)。GPU 就是典型的「牺牲延迟换极致吞吐」——
      单个任务不快,但能同时跑几千个。</p>`},

    {t:"warn", h:"⚠️ 流水线的三种「翻车」(冒险 hazards)", html:`
      <p>流水线不是白吃的午餐。它假设「指令之间互不干扰」,但现实经常打脸:</p>
      <p><b>1. 数据冒险:下一条要用上一条还没算完的结果。</b></p>
      <pre><code>a = b + c;   // 这条还在流水线里没写回
d = a + e;   // 这条马上就要用 a —— 只能等!(叫 stall/气泡)</code></pre>
      <p>硬件用「转发/前递(forwarding)」缓解:结果一算出来就抄近道直接送给下一条,不等它写回寄存器。</p>
      <p><b>2. 控制冒险:遇到 if 分支,不知道下一条该取哪边。</b>
      流水线不能停下来等,于是它<b>赌</b>一把 —— 这引出下面最重要的一段。</p>
      <p><b>3. 结构冒险:两条指令抢同一个硬件部件。</b>靠增加部件或错开时序解决。</p>`},

    {t:"text", h:"🎲 分支预测:CPU 是个赌徒", html:`
      <p>控制冒险最要命。CPU 执行到 <code>if (x > 0)</code> 时,分支结果还没算出来,
      但流水线不能空转等着(那就白白浪费好几个周期)。怎么办?</p>
      <p><b>CPU 直接赌一个方向,先把猜中的那条路的指令灌进流水线继续跑。</b>
      现代 CPU 的<b>分支预测器</b>会记录历史规律(「这个循环前999次都是继续」),准确率高达95%以上。</p>
      <ul>
        <li><b>猜对了</b>:完美,流水线一刻没停,零代价。</li>
        <li><b>猜错了</b>:糟糕。已经灌进去的十几条指令全是错的,得<b>整个倒掉(flush)重来</b>,
        浪费十几个周期。这叫<b>分支预测失败惩罚</b>。</li>
      </ul>
      <blockquote>🎯 这解释了一个著名的性能现象:<b>处理「已排序数组」常常比「乱序数组」快好几倍</b>,
      即使代码一模一样。因为排序后 <code>if (x > 阈值)</code> 的结果连续可预测(前半全假、后半全真),
      预测器几乎不失手;乱序时结果像抛硬币,预测器频繁猜错,流水线反复倒掉。</blockquote>
      <p>实用推论:性能敏感的热点循环里,<b>可预测的分支</b>(甚至用无分支写法/查表代替 if)有时能显著提速。
      但同样,别过早优化 —— 先测量,确认这里真是瓶颈再动手。</p>`}
  ],
  quiz:[
    {id:"w8bq1", q:"流水线为什么能提升性能?",
     opts:["让每条指令执行得更快","让不同指令的不同阶段用不同硬件部件并行处理,提高吞吐量","减少了指令数量","跳过了某些阶段"],
     answer:1, ex:"5个阶段用不同部件,指令1在EX时指令2可以在ID、指令3在IF,同时推进。单条指令延迟没变,但单位时间流出的指令数(吞吐)大增。"},
    {id:"w8bq2", q:"关于「延迟」和「吞吐量」,哪句是对的?",
     opts:["它们是一回事","延迟是单件事的耗时,吞吐是单位时间完成的件数;流水线提升吞吐但不降低单条延迟","流水线降低延迟","吞吐量越高延迟一定越低"],
     answer:1, ex:"延迟=一件事多久完成，吞吐=单位时间完成多少。流水线让单条指令延迟不变(还是5级)，但吞吐提升。快递比喻：单件1天到(延迟)，每天百万件(吞吐)。"},
    {id:"w8bq3", q:"「数据冒险」指的是什么?",
     opts:["数据丢失","一条指令需要用到前面指令尚未算完/写回的结果,被迫等待","内存不够","缓存未命中"],
     answer:1, ex:"后一条依赖前一条还在流水线中的结果，只能停顿(气泡)。硬件用「转发」缓解：结果一出来就抄近道直接送给需要它的指令，不等写回寄存器。"},
    {id:"w8bq4", q:"分支预测失败(猜错)的代价是什么?",
     opts:["程序出错","已经进入流水线的错误指令必须全部作废重来,浪费十几个周期","数据被破坏","CPU重启"],
     answer:1, ex:"预测错时，为那条路预取并推进的十几条指令全是错的，必须flush清空、从正确方向重新取指，浪费大量周期。所以预测器准确率至关重要。"},
    {id:"w8bq5", q:"为什么处理「已排序数组」常比「乱序数组」快,即使代码相同?",
     opts:["排序后数据更少","排序后分支(如 if x>阈值)结果连续可预测,分支预测几乎不失手;乱序时频繁预测失败导致流水线反复清空","排序改变了算法复杂度","缓存对排序数组更友好"],
     answer:1, ex:"经典现象。排序后 if 条件呈现「连续假、连续真」的规律，预测器命中率极高；乱序时结果像抛硬币，预测频繁失败，每次都要清空流水线，累积成数倍差距。"}
  ]
};

WORLDS.push({id:"w8", num:"世界 8", icon:"🚀", name:"让它更快", sub:"缓存、流水线与性能直觉", nodes:["w8a","w8b"], badge:"cache_hit"});

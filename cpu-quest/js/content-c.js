/* ============ 世界5：指令集 ============ */
LEVELS["w5a"] = {
  icon:"📜", title:"CPU 的母语：指令集", xp:100,
  hook:"软硬件之间的那份合同",
  blocks:[
    {t:"story", h:"📖 一份合同", html:`
      <p>CPU 造好了。但它怎么知道该干什么？</p>
      <p>你得给它一串<b>命令</b>。而命令必须写成它认识的格式 —— 因为「认识」的意思，
      就是「CPU 里有一片译码电路，恰好能把这串比特翻译成正确的控制信号」。</p>
      <p>这套格式的完整规定，叫 <b>指令集架构（ISA，Instruction Set Architecture）</b>。</p>
      <p><b>ISA 是整座塔的腰。</b>它上面是软件，下面是硬件。它是一份合同：</p>
      <blockquote>硬件承诺：你给我这样的比特，我就做这件事。<br>
      软件承诺：我只用你列出的这些指令。</blockquote>
      <p>只要合同不变，Intel 可以年年重新设计芯片内部（微架构），
      而你 20 年前编译的程序<b>今天还能跑</b>。这份合同的稳定性，是整个软件产业的地基。</p>`},

    {t:"text", h:"🗂️ 指令其实只有四类", html:`
      <p>x86 有上千条指令，听起来很吓人。但按功能分，只有四类：</p>
      <table>
        <tr><th>类别</th><th>干什么</th><th>例子</th><th>对应你写的代码</th></tr>
        <tr><td><b>数据搬运</b></td><td>把数据从A搬到B</td><td>MOV, LOAD, STORE, PUSH</td><td><code>x = y</code></td></tr>
        <tr><td><b>算术逻辑</b></td><td>算数、比较</td><td>ADD, SUB, MUL, AND, CMP</td><td><code>a + b</code></td></tr>
        <tr><td><b>控制转移</b></td><td>改变 PC，不按顺序走</td><td>JMP, JE, CALL, RET</td><td><code>if</code>, <code>for</code>, 函数调用</td></tr>
        <tr><td><b>系统/特殊</b></td><td>跟操作系统或硬件打交道</td><td>SYSCALL, INT, HLT</td><td><code>printf</code>, 读文件</td></tr>
      </table>
      <p><b>就这四类。</b>你写的任何程序 —— 神经网络、数据库、编译器 ——
      最终都是这四类指令的排列组合。</p>`},

    {t:"text", h:"🔤 一条指令长什么样", html:`
      <p>指令本身也是比特。比如某个虚构 ISA 规定 16 位指令格式：</p>
      <pre><code>┌──────┬──────┬──────┬──────┐
│ 0001 │ 0010 │ 0011 │ 0100 │
├──────┼──────┼──────┼──────┤
│ 操作码│ 目标 │ 源1  │ 源2  │
│ ADD  │  R2  │  R3  │  R4  │
└──────┴──────┴──────┴──────┘
含义：R2 = R3 + R4</code></pre>
      <p>控制单元的工作就是：看前 4 位（操作码）→ 知道是 ADD →
      拉高「ALU 做加法」的控制线，拉高「读 R3、读 R4、写 R2」的控制线。<b>纯粹的组合逻辑。</b></p>
      <p>再次强调世界1那句话：<b>比特没有内在含义。</b>
      同一串比特，PC 指向它时它就是指令，数据指针指向它时它就是数据。
      （这也是缓冲区溢出攻击的原理：把数据塞到会被当成指令执行的地方。）</p>`},

    {t:"aha", h:"💡 冯·诺依曼结构：指令和数据住一起", html:`
      <p>1945 年冯·诺依曼提出了一个当时很激进的想法：<b>把程序和数据放在同一块内存里。</b></p>
      <p>好处巨大：</p>
      <ul>
        <li>程序变成了「数据」，所以程序可以被存储、复制、传输 —— <b>软件产业成立了</b></li>
        <li>程序可以生成程序 —— 这就是编译器、JIT、虚拟机的基础</li>
        <li>硬件只需要一套内存和一条总线，成本低</li>
      </ul>
      <p>坏处也在这：</p>
      <ul>
        <li><b>冯·诺依曼瓶颈</b>：指令和数据挤同一条总线，抢带宽（现代 CPU 用分离的 L1i/L1d 缓存缓解）</li>
        <li><b>安全灾难</b>：数据能被当指令执行 → 缓冲区溢出、代码注入。
        现代防御手段（NX 位标记内存不可执行、ASLR 随机化地址）都是在给这个设计打补丁。</li>
      </ul>`},

    {t:"text", h:"⚔️ CISC vs RISC：两种哲学", html:`
      <table>
        <tr><th></th><th>CISC（x86）</th><th>RISC（ARM / RISC-V）</th></tr>
        <tr><td>指令数量</td><td>上千条，有些很复杂</td><td>精简，指令简单</td></tr>
        <tr><td>指令长度</td><td>变长（1~15字节）</td><td>定长（通常4字节）</td></tr>
        <tr><td>一条指令能干</td><td>可能很多（如直接操作内存）</td><td>一件事（访存和运算分开）</td></tr>
        <tr><td>译码难度</td><td>难（长度都得先猜）</td><td>简单，容易并行</td></tr>
        <tr><td>典型场景</td><td>PC、服务器</td><td>手机、嵌入式、Apple M系列</td></tr>
      </table>
      <p><b>有意思的转折：现代 x86 内部其实是 RISC。</b>它前端有个「解码器」把复杂的 x86 指令
      拆成简单的<b>微操作（micro-ops）</b>，内部用类似 RISC 的方式执行。
      所以 CISC 只剩下一层<b>兼容性外壳</b>了 —— 保留它纯粹是因为几十亿行现存软件。</p>
      <p>这正好又是一个「抽象」的例子：x86 这个<b>接口</b>没变，
      但底下的<b>实现</b>已经换了几代人。</p>`}
  ],
  quiz:[
    {id:"w5aq1", q:"指令集架构（ISA）本质上是什么？",
     opts:["一种编程语言","软件和硬件之间的接口约定（合同）","CPU 的物理结构","操作系统的一部分"],
     answer:1, ex:"ISA 是接口/合同：规定了 CPU 认识哪些指令、怎么编码。它稳定，所以硬件内部可以彻底重新设计而老软件照跑。"},
    {id:"w5aq2", q:"冯·诺依曼结构最关键的特点是？",
     opts:["有多个 CPU 核心","指令和数据存放在同一个内存里","使用二进制","有缓存"],
     answer:1, ex:"程序存储在可读写内存里，跟数据同居。这让程序变成了可存储可传输的数据（软件产业的基础），但也带来了总线瓶颈和代码注入的安全问题。"},
    {id:"w5aq3", q:"下面哪一类指令对应你代码里的 if 和 for？",
     opts:["数据搬运类","算术逻辑类","控制转移类","系统调用类"],
     answer:2, ex:"控制转移类（JMP/JE/CALL）改变 PC 的值，让程序不按顺序走。所有分支和循环底层都是「比较 + 条件跳转」。"},
    {id:"w5aq4", q:"关于现代 x86 处理器，哪句话是对的？",
     opts:["它是纯粹的 CISC 实现","它把 x86 指令解码成类似 RISC 的微操作在内部执行","它已经改用 ARM 指令集","它不支持老程序"],
     answer:1, ex:"现代 x86 前端把复杂指令拆成 micro-ops，内部按 RISC 风格执行。CISC 只是保留的兼容外壳 —— 又一个「接口不变、实现换代」的抽象案例。"}
  ]
};

LEVELS["w5b"] = {
  icon:"▶️", title:"让它跑起来！单步执行", xp:130,
  hook:"这一关你会亲眼看到 CPU 在思考",
  blocks:[
    {t:"story", h:"📖 通电时刻", html:`
      <p>零件齐了，指令格式也定了。<b>上电。</b></p>
      <p>下面这台迷你 CPU 是真的能跑的。它在计算 <code>1+2+3+4+5</code>，
      而你可以<b>一个微步一个微步</b>地看它怎么做到。</p>
      <p>这是整个网站最重要的一关。<b>请务必动手点完。</b>
      看懂这个循环，你对计算机的理解就跨过了最大的那道坎。</p>`},

    {t:"text", h:"📋 先读懂这段程序", html:`
      <pre><code>0: LOAD R1, #5    ; R1 = 5      （计数器，从5数到0）
1: LOAD R2, #0    ; R2 = 0      （累加器，存总和）
2: ADD  R2, R1    ; R2 = R2 + R1（把当前值加进总和）
3: DEC  R1        ; R1 = R1 - 1 （计数器减1）
4: JNZ  R1, 2     ; 如果 R1 ≠ 0，跳回第2行
5: OUT  R2        ; 输出 R2
6: HALT           ; 停机</code></pre>
      <p>第 2~4 行是一个<b>循环</b>。注意它是怎么循环的：</p>
      <blockquote><b>没有 for，没有 while。只有「减一 + 判断 + 改 PC」。</b><br>
      你写的每一个 <code>for</code>，编译后都长这样。</blockquote>
      <p>换成 C 语言，这段程序就是：</p>
      <pre><code>int r1 = 5, r2 = 0;
do { r2 += r1; r1--; } while (r1 != 0);
printf("%d", r2);   // 15</code></pre>`},

    {t:"lab", h:"⚡⚡ 实验室：迷你 CPU（本站核心）", lab:"minicpu",
     html:`<p><b>操作指南：</b></p>
     <ol>
       <li>先点 <b>「下一步」</b> 十几次，慢慢看。注意上方三个阶段灯的轮转：取指 → 译码 → 执行。</li>
       <li>盯住 <b>PC</b>：它平时是 +1 +1 地走，但执行到 JNZ 时会<b>突然被改成 2</b> —— 那一刻就是「循环」的物理真相。</li>
       <li>盯住 <b>R2</b>：5 → 9 → 12 → 14 → 15。它在攒钱。</li>
       <li>看烦了就点 <b>「自动跑」</b>，看它自己跑完。</li>
     </ol>
     <p>跑完会有奖励徽章 🏆</p>`},

    {t:"aha", h:"💡 你刚刚看到的是什么", html:`
      <p>你刚刚看到的，是<b>计算的全部本质</b>。真的，没有夸张。</p>
      <p>你的电脑此刻正在做的事，和你刚点的那台迷你 CPU <b>在原理上完全一致</b>。区别只有：</p>
      <table>
        <tr><th></th><th>刚才那台</th><th>你的电脑</th></tr>
        <tr><td>寄存器数量</td><td>2 个</td><td>16~32 个（外加几百个物理重命名寄存器）</td></tr>
        <tr><td>指令种类</td><td>6 种</td><td>上千种</td></tr>
        <tr><td>速度</td><td>你手点多快就多快</td><td>每秒几十亿个周期</td></tr>
        <tr><td>同时执行</td><td>1 条</td><td>几十条（流水线+乱序+多核）</td></tr>
      </table>
      <p><b>但循环还是那个循环：取指 → 译码 → 执行。</b></p>
      <p>浏览器渲染这个页面、AI 生成文字、游戏画一帧 —— 全都是这个循环，
      只是指令多得数不清而已。<b>没有任何魔法被隐藏起来。</b></p>`},

    {t:"text", h:"🔀 分支：if 的真相", html:`
      <p>你写：</p>
      <pre><code>if (a > b) { x = 1; } else { x = 2; }</code></pre>
      <p>CPU 看到的是：</p>
      <pre><code>    CMP  a, b        ; 做减法 a-b，设置标志位（不保存结果）
    JLE  else_part   ; 如果「小于或等于」标志成立，跳到 else_part
    MOV  x, 1        ; then 分支
    JMP  end         ; 跳过 else，否则会顺着执行下去！
else_part:
    MOV  x, 2
end:</code></pre>
      <p><b>三个关键领悟：</b></p>
      <ol>
        <li><b>没有「if」这个指令。</b>只有「比较」+「条件跳转」两步。</li>
        <li><b>then 分支末尾必须有一个无条件 JMP</b>，否则代码会顺着流进 else。
        这就是为什么忘记 <code>break</code> 会导致 switch 穿透 —— 汇编层面它本来就是顺着流的。</li>
        <li><b>CMP 其实就是 SUB，只是不保存结果，只要标志位。</b></li>
      </ol>
      <p>循环也一样：<code>for</code> 就是「初始化 + 循环体 + 递增 + 比较 + 跳回开头」。
      <b>所有控制流，归根结底都是在改 PC 的值。</b></p>`}
  ],
  quiz:[
    {id:"w5bq1", q:"在迷你 CPU 里，「循环」这件事物理上是怎么实现的？",
     opts:["有专门的循环电路","条件跳转指令把 PC 改成一个更早的地址","控制单元记住了循环次数","编译器展开了循环"],
     answer:1, ex:"就是改 PC。PC 平时+1顺序走，跳转指令直接往 PC 里塞一个旧地址，于是又从那里执行起来。所有循环、分支、函数调用本质都是改 PC。"},
    {id:"w5bq2", q:"CMP（比较）指令实际上在做什么？",
     opts:["逐位比较两个数","做减法但丢弃结果，只保留标志位","查表判断大小","调用比较函数"],
     answer:1, ex:"CMP 就是 SUB，只是不写回结果，只更新 Z/N/C 等标志位。随后的条件跳转指令根据这些标志决定跳不跳。"},
    {id:"w5bq3", q:"if-else 编译成汇编后，为什么 then 分支末尾必须加一条无条件跳转？",
     opts:["为了性能优化","否则代码会顺序执行下去，流进 else 分支","为了对齐内存","编译器习惯"],
     answer:1, ex:"指令默认顺序执行。then 执行完必须主动跳过 else，不然就会把 else 也执行了。这也解释了 switch 里忘写 break 的穿透现象。"},
    {id:"w5bq4", q:"CPU 执行指令的三个阶段，正确顺序是？",
     opts:["译码 → 取指 → 执行","取指 → 执行 → 译码","取指 → 译码 → 执行","执行 → 取指 → 译码"],
     answer:2, ex:"Fetch（按PC取指令进IR）→ Decode（控制单元看懂它）→ Execute（干活并更新PC），然后无限重复。"}
  ]
};

WORLDS.push({id:"w5", num:"世界 5", icon:"📜", name:"CPU 的母语：指令集", sub:"上电，让它真的跑起来", nodes:["w5a","w5b"], badge:"first_run"});

/* ============ 世界6：从人话到机器话 ============ */
LEVELS["w6a"] = {
  icon:"⚙️", title:"gcc 偷偷做的 5 件事", xp:100,
  hook:"一条命令背后的完整流水线",
  blocks:[
    {t:"story", h:"📖 中间的鸿沟", html:`
      <p>现在有个很现实的落差：</p>
      <p>CPU 只认 <code>89 f8 01 f0 c3</code> 这种东西。而你想写的是 <code>return a + b;</code>。</p>
      <p>中间这段翻译，是几十年来最多人参与的工程之一。<b>它分五步。</b></p>`},

    {t:"lab", h:"⚡ 实验室：编译流水线（点标签逐步看）", lab:"compile"},

    {t:"text", h:"📚 汇编语言：给机器码起外号", html:`
      <p>机器码 <code>89 f8</code> 没人记得住。所以有人给每个操作码起了个人类外号：</p>
      <table class="tbl-mono">
        <tr><th>机器码</th><th>汇编</th><th>意思</th></tr>
        <tr><td>89 f8</td><td>mov eax, edi</td><td>把 edi 的值复制到 eax</td></tr>
        <tr><td>01 f0</td><td>add eax, esi</td><td>eax = eax + esi</td></tr>
        <tr><td>c3</td><td>ret</td><td>函数返回</td></tr>
      </table>
      <p><b>汇编和机器码基本是一对一的。</b>汇编器（assembler）做的是查表替换，不做「翻译」。
      所以汇编不算真正的高级语言 —— 它只是机器码的<b>可读写法</b>。</p>
      <p><b>今天还需要学汇编吗？</b>不需要用它写程序。但需要<b>能读</b>：
      调试崩溃、看编译器优化了什么、做性能分析时都得读。会读不会写，是现在的常态。</p>`},

    {t:"text", h:"🔗 链接：最容易报错的一步", html:`
      <p>大项目有几百个 <code>.c</code> 文件，每个单独编译成 <code>.o</code>。
      问题是：<code>a.c</code> 里调用了 <code>b.c</code> 里的函数，
      编译 <code>a.c</code> 的时候<b>根本不知道那个函数最终会在哪个地址</b>。</p>
      <p>所以编译器先在那里留个空（<code>call ????</code>），记在一张「我还欠这些符号」的表里。
      <b>链接器</b>最后把所有 <code>.o</code> 拼在一起，算出每个函数的最终地址，回来把所有空填上。这叫<b>重定位</b>。</p>
      <p><b>所以这两个报错完全是两码事：</b></p>
      <ul>
        <li><code>error: 'foo' undeclared</code> —— <b>编译期</b>错误。你没告诉编译器 foo 长什么样（缺头文件声明）。</li>
        <li><code>undefined reference to 'foo'</code> —— <b>链接期</b>错误。声明有了，但<b>实现</b>找不到
        （忘了编译某个 .c、忘了 <code>-lm</code> 链接数学库）。</li>
      </ul>
      <p>搞清这个区别，能省下你未来大量抓瞎的时间。</p>`},

    {t:"aha", h:"💡 静态链接 vs 动态链接", html:`
      <p><b>静态链接</b>：把库的代码整个复制进你的可执行文件。</p>
      <p>→ 文件大，但拿到哪都能跑（Go 默认这样，所以 Go 程序部署很爽）。</p>
      <p><b>动态链接</b>：只记「我需要 libc.so」，运行时才去系统里找并加载。</p>
      <p>→ 文件小，多个程序共享同一份库、库升级不用重编译。<b>代价是依赖地狱。</b></p>
      <p>你见过的这些错误，全是动态链接的锅：</p>
      <ul>
        <li>Linux：<code>error while loading shared libraries: libxxx.so.6: cannot open</code></li>
        <li>Windows：<code>找不到 XXX.dll</code></li>
        <li>Python：<code>ImportError: libcudnn.so.8: cannot open shared object file</code></li>
      </ul>
      <p>Docker 之所以流行，很大一部分原因就是：<b>把整个依赖环境打包，绕过依赖地狱。</b></p>`},

    {t:"text", h:"🎁 编译器优化：它比你想的聪明", html:`
      <p>你写：</p>
      <pre><code>int sum = 0;
for (int i = 1; i <= 100; i++) sum += i;
return sum;</code></pre>
      <p>开 <code>-O2</code> 后 gcc 生成的汇编可能是：</p>
      <pre><code>mov eax, 5050
ret</code></pre>
      <p><b>循环消失了。</b>编译器在编译期就把结果算出来了（常量折叠 + 循环消除）。</p>
      <p>常见优化手段：</p>
      <ul>
        <li><b>常量折叠</b>：<code>2*3</code> 直接变成 <code>6</code></li>
        <li><b>死代码消除</b>：算了但没人用的，删掉</li>
        <li><b>内联展开</b>：小函数直接把代码贴进调用处，省掉调用开销</li>
        <li><b>循环展开</b>：把循环体复制几份，减少判断次数（利于流水线）</li>
        <li><b>寄存器分配</b>：尽量让变量待在寄存器里而不是内存</li>
        <li><b>强度削弱</b>：<code>x*8</code> 变成 <code>x<<3</code>（移位比乘法快）</li>
      </ul>
      <p><b>实用推论：不要做「聪明」的微优化。</b>你手写的位运算技巧，编译器大概率已经做了，
      而你的版本还更难读。<b>先测量，再优化，只优化热点。</b></p>
      <p>想亲眼看？终端里跑：<code>gcc -O2 -S test.c -o -</code>，或者上 godbolt.org。</p>`}
  ],
  quiz:[
    {id:"w6aq1", q:"「undefined reference to 'foo'」是哪个阶段的错误？",
     opts:["预处理","编译","链接","运行"],
     answer:2, ex:"链接期。说明编译器知道 foo 长什么样（有声明），但链接器找不到它的实现 —— 通常是忘了编译某个源文件或忘了链接某个库。"},
    {id:"w6aq2", q:"预处理（#include）做的是什么？",
     opts:["检查语法正确性","纯文本替换：把头文件内容原地展开、替换宏、删注释","生成机器码","优化代码"],
     answer:1, ex:"预处理器不懂 C 语法，它只做文本操作。#include 就是「把那个文件的内容整个粘贴到这一行」，所以头文件重复包含才需要 include guard。"},
    {id:"w6aq3", q:"动态链接相比静态链接，主要优势是？",
     opts:["运行速度更快","可执行文件更小，多程序共享库，库升级无需重新编译","更安全","没有依赖问题"],
     answer:1, ex:"省空间、可共享、可独立升级。代价是运行时依赖 —— 「找不到 xxx.so / xxx.dll」这类错误就是这么来的，也是 Docker 流行的重要原因之一。"},
    {id:"w6aq4", q:"关于编译器优化，正确的做法是？",
     opts:["尽量手写位运算等技巧代替普通写法","先写清晰的代码，测量后只优化真正的热点","关闭优化以保证正确性","把所有循环手动展开"],
     answer:1, ex:"编译器的常量折叠、内联、强度削弱等优化通常比手写技巧更好。可读性优先，靠 profiler 找到热点再针对性优化。"}
  ]
};

WORLDS.push({id:"w6", num:"世界 6", icon:"⚙️", name:"从人话到机器话", sub:"编译、汇编、链接的全过程", nodes:["w6a"]});

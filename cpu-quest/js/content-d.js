/* ============ 世界7：程序运行的舞台（内存 / 指针 / 栈） ============ */
LEVELS["w7a"] = {
  icon:"🎭", title:"变量到底住在哪", xp:110,
  hook:"一个程序跑起来，内存被切成了六块",
  blocks:[
    {t:"story", h:"📖 一个你从没问过的问题", html:`
      <p>你写了一辈子 <code>int x = 5;</code>。但你有没有想过：<b>这个 5，到底放在哪？</b></p>
      <p>「内存里」—— 对，但内存不是一大团均匀的沙子。程序一跑起来，
      操作系统就把它的内存<b>切成了六块，每块有各自的脾气</b>。</p>
      <p>搞不懂这六块，你就永远搞不懂：为什么局部变量出了函数就没了、
      为什么 <code>malloc</code> 的东西不 <code>free</code> 会泄漏、为什么全局变量能被所有函数看到、
      为什么字符串常量改不得。<b>这一关把这六块一次性讲透。</b></p>`},

    {t:"lab", h:"⚡ 实验室：一个进程的内存地图", lab:"memmap",
     html:`<p>先看全景（鼠标悬停每一块看说明）。<b>注意两个箭头：栈向下长，堆向上长，
     它们朝着对方生长，中间那块空地就是它们的活动空间。</b></p>`},

    {t:"text", h:"🧱 六块，从下往上", html:`
      <table>
        <tr><th>段</th><th>放什么</th><th>谁管理</th><th>生命周期</th></tr>
        <tr><td><b>Text 代码段</b></td><td>你的机器指令（只读）</td><td>加载器</td><td>整个程序</td></tr>
        <tr><td><b>Data</b></td><td>初始化过的全局/静态变量</td><td>加载器</td><td>整个程序</td></tr>
        <tr><td><b>BSS</b></td><td>没初始化的全局/静态（自动清0）</td><td>加载器</td><td>整个程序</td></tr>
        <tr><td><b>Heap 堆</b></td><td><code>malloc/new</code> 出来的</td><td><b>你自己</b>（或GC）</td><td>你说了算</td></tr>
        <tr><td><b>Stack 栈</b></td><td>局部变量、参数、返回地址</td><td>编译器自动</td><td>函数进/出</td></tr>
      </table>
      <p>用一段代码把它们全标出来：</p>
      <pre><code>int g = 42;              // Data（初始化了）
int uninit;              // BSS（没初始化）
const char *s = "hi";    // 指针s在栈，"hi"在只读区

void f() {
    int local = 1;       // Stack（栈）
    static int cnt = 0;  // Data！虽在函数里，但活整个程序
    int *p = malloc(8);  // p在栈，p指向的8字节在Heap（堆）
}</code></pre>
      <p><b>盯住最后两行：</b>指针变量 <code>p</code> 本身住在栈上（就 8 字节，存一个地址），
      而它<b>指向</b>的那块内存在堆上。这个「指针在栈、数据在堆」的分离，是理解指针的钥匙。</p>`},

    {t:"aha", h:"💡 栈 vs 堆：一张表说清所有区别", html:`
      <table>
        <tr><th></th><th>栈 Stack</th><th>堆 Heap</th></tr>
        <tr><td>谁分配/释放</td><td>编译器自动（函数进出）</td><td>你手动 malloc/free</td></tr>
        <tr><td>速度</td><td>极快（就移动一个指针）</td><td>慢（要找空闲块、维护记账）</td></tr>
        <tr><td>大小</td><td>小（通常 1~8MB）</td><td>大（可到几GB）</td></tr>
        <tr><td>生命周期</td><td>出了作用域就没</td><td>直到你 free</td></tr>
        <tr><td>典型翻车</td><td>栈溢出、返回局部变量地址</td><td>内存泄漏、悬空指针、double free</td></tr>
      </table>
      <p><b>一句话决策：</b>小的、短命的、离开函数就不要的 → 栈（默认就是）。
      大的、要活过函数、大小运行时才知道的 → 堆(<code>malloc</code>)。</p>
      <p>为什么栈快得离谱？分配一个栈变量，CPU <b>只是把栈指针寄存器减一下</b>，一条指令的事。
      堆分配却要去一个数据结构里找「哪块空地够大」，还要更新记账信息 —— 慢几十倍很正常。
      这就是「能用栈就别用堆」这条性能经验的由来。</p>`},

    {t:"warn", h:"⚠️ 指针：C 语言最锋利也最容易割手的刀", html:`
      <p>指针就是「一个存着地址的变量」。就这么简单。但围绕它有一堆经典事故：</p>
      <p><b>1. 返回局部变量的地址（最经典的坑）</b></p>
      <pre><code>int* bad() {
    int x = 5;
    return &x;   // ❌ 返回栈上 x 的地址
}                // 函数一返回，x 所在的栈帧就被回收了
                 // 你拿到的是一张「已退房房间」的钥匙</code></pre>
      <p><b>2. 悬空指针（dangling pointer）</b>：<code>free(p)</code> 之后 <code>p</code> 还指着那块地，
      再用就是访问已归还的内存。养成 <code>free(p); p = NULL;</code> 的习惯。</p>
      <p><b>3. 空指针解引用</b>：<code>p = NULL; *p;</code> → 段错误。地址 0 是操作系统故意留的雷区。</p>
      <p><b>4. 越界</b>：<code>int a[10]; a[10] = 1;</code> —— 数组下标从 0 到 9，
      <code>a[10]</code> 已经踩到别人家了。C 不检查，所以缓冲区溢出成了头号安全漏洞。</p>`},

    {t:"text", h:"🔍 「段错误 Segmentation fault」到底是什么", html:`
      <p>这个让无数人头秃的报错，其实含义很精确：<b>你访问了一个操作系统没允许你访问的内存地址。</b></p>
      <p>操作系统给每一段内存标了权限（可读?可写?可执行?）。你一旦：</p>
      <ul>
        <li>解引用空指针（碰地址 0 附近的雷区）</li>
        <li>写只读区（比如改字符串常量 <code>char *s="hi"; s[0]='H';</code>）</li>
        <li>访问根本没映射的地址（野指针）</li>
        <li>栈无限增长撞穿边界（无限递归）</li>
      </ul>
      <p>硬件（MMU）当场发现越权，触发一个异常，操作系统收到后把你的进程<b>当场击毙</b>，
      打印 Segmentation fault。<b>它其实是在保护你</b> —— 总比让你悄悄改坏别人的数据、
      过几小时才莫名崩溃要好。这套地址权限检查的机制，正是世界9「虚拟内存」的核心。</p>`}
  ],
  quiz:[
    {id:"w7aq1", q:"下面代码里，指针 p 和它指向的 8 字节数据分别在哪？",
     opts:["都在栈","都在堆","p 在栈，数据在堆","p 在堆，数据在栈"],
     answer:2, ex:"int *p = malloc(8): p 是个局部变量（住栈上，存一个地址），malloc 返回的 8 字节在堆上。指针在栈、数据在堆的分离是理解指针的关键。"},
    {id:"w7aq2", q:"为什么「返回局部变量的地址」是危险的？",
     opts:["局部变量值会变","函数返回后其栈帧被回收，该地址指向的内存已失效","局部变量太小","编译器不允许"],
     answer:1, ex:"局部变量在栈帧里，函数一返回栈帧就被回收（栈指针弹回去）。返回的地址像一张已退房房间的钥匙，之后那块内存随时被别的调用覆盖。"},
    {id:"w7aq3", q:"栈内存分配为什么比堆快得多？",
     opts:["栈更靠近 CPU","栈分配只需移动栈指针一条指令，堆要查找空闲块并记账","栈用了缓存","堆需要联系操作系统"],
     answer:1, ex:"栈分配就是把栈指针加减一个数，一条指令。堆分配要在数据结构里搜索合适的空闲块、更新元数据，慢几十倍。所以「能用栈就别用堆」。"},
    {id:"w7aq4", q:"「Segmentation fault」本质上是什么？",
     opts:["内存不够了","程序访问了未被授权的内存地址，被硬件和OS拦下","栈溢出了","指针类型错误"],
     answer:1, ex:"访问越权内存（碰空指针雷区、写只读区、访问未映射地址等），MMU 硬件当场发现，OS 终止进程。它其实在保护你，避免更隐蔽的破坏。"},
    {id:"w7aq5", q:"static 局部变量存在哪个段？",
     opts:["栈","堆","Data/BSS 段（和全局变量一起）","代码段"],
     answer:2, ex:"static 变量虽然写在函数里、只有函数内可见，但它活整个程序生命周期，所以存在 Data（初始化了）或 BSS（没初始化）段，不在栈上。这就是它能「记住上次的值」的原因。"}
  ]
};

LEVELS["w7b"] = {
  icon:"🥞", title:"函数调用栈：程序的记忆链", xp:120,
  hook:"递归、返回、栈溢出，都在这一张图里",
  blocks:[
    {t:"story", h:"📖 一个组织难题", html:`
      <p>函数 A 调用 B，B 又调用 C。等 C 干完，怎么知道该回到 B 的哪一行？B 干完又怎么回到 A？</p>
      <p>而且每个函数都有自己的局部变量，它们不能互相打架。<b>CPU 只有十几个寄存器，怎么装得下这么多层的现场？</b></p>
      <p>答案是一个优雅到你会拍大腿的数据结构：<b>栈</b>。后进先出,正好匹配「最后调用的最先返回」。</p>`},

    {t:"text", h:"📦 栈帧：每个函数的私人抽屉", html:`
      <p>每次调用一个函数，就在栈上压入一个 <b>栈帧（stack frame）</b>，里面装着这次调用的全部现场：</p>
      <ul>
        <li><b>返回地址</b>：干完回到调用者的哪一行（PC 要恢复成什么）</li>
        <li><b>参数</b>：传进来的实参</li>
        <li><b>局部变量</b>：函数内定义的变量</li>
        <li><b>保存的寄存器</b>：调用前的现场，返回时要还原</li>
      </ul>
      <p>函数返回时，这个帧被<b>整个弹掉</b>，栈指针跳回去，控制权和现场一起恢复到调用者。
      「局部变量出了函数就消失」的物理真相就是这个 —— 帧没了，里面的东西自然全没。</p>`},

    {t:"lab", h:"⚡ 实验室：递归调用栈（单步）", lab:"stack",
     html:`<p>这里跑 <code>fact(4)</code>（阶乘）。点「下一步」：</p>
     <ol>
       <li>先看栈<b>一层层长高</b>：fact(4)→fact(3)→fact(2)→fact(1)。每次调用压一个帧。</li>
       <li>到 fact(1) 触底后，看栈<b>一层层塌回去</b>：每个函数拿到下层的返回值，算出自己的结果再弹栈。</li>
       <li>最终 fact(4)=24。<b>注意先压的最后弹</b> —— 这就是「后进先出」。</li>
     </ol></p>`},

    {t:"aha", h:"💡 栈溢出：叠太高的塔", html:`
      <p>栈空间是有限的（通常 1~8MB）。如果帧压得太多太深，就会撞穿栈的边界 → <b>Stack Overflow</b>
      （对，那个著名网站就叫这个）。最常见的两个原因：</p>
      <pre><code>// 1. 递归忘了终止条件 —— 无限压帧
int boom(int n){ return boom(n+1); }  // 永不返回

// 2. 在栈上开巨大的数组
void f(){ int huge[10000000]; }       // 一个帧就想吃掉几十MB</code></pre>
      <p><b>怎么救？</b></p>
      <ul>
        <li>递归改循环（很多递归能改成迭代，不吃栈）</li>
        <li>把大数组放堆上（<code>malloc</code>）而不是栈上</li>
        <li>确保递归有正确的、一定能到达的终止条件</li>
      </ul>
      <p>顺带：<b>尾递归优化</b> —— 如果递归调用是函数的最后一步，
      有些编译器能把它复用同一个栈帧（不再叠高），让递归变得和循环一样省栈。
      (C 不保证，函数式语言如 Scheme 强制保证。)</p>`},

    {t:"warn", h:"⚠️ 栈溢出攻击：黑客的经典入口", html:`
      <p>记得吗——<b>返回地址也在栈上</b>。而栈上还有你的局部数组。如果程序往数组里写数据时不检查长度……</p>
      <pre><code>void login(char *input){
    char buf[16];
    strcpy(buf, input);   // ❌ 不检查长度！
}</code></pre>
      <p>攻击者传一个超过 16 字节的 <code>input</code>，多出来的部分就会<b>顺着栈往上,把返回地址覆盖掉</b>。
      精心构造的话，返回地址会被改成指向攻击者自己塞进去的代码 —— 函数一「返回」,
      CPU 就跳去执行黑客的代码了。这就是<b>缓冲区溢出攻击（buffer overflow）</b>,
      几十年来最经典的漏洞类型。</p>
      <p>现代防御（都是在给冯·诺依曼结构打补丁）：</p>
      <ul>
        <li><b>栈保护（canary）</b>：在返回地址前放个「哨兵值」，返回前检查它有没有被改动</li>
        <li><b>NX/DEP</b>：把栈标记为「不可执行」，就算注入了代码也跑不起来</li>
        <li><b>ASLR</b>：每次运行随机化内存布局，让攻击者猜不到地址</li>
      </ul>
      <p>所以为什么要用 <code>strncpy</code> 而不是 <code>strcpy</code>、为什么现代语言（Rust/Go/Java）
      默认检查数组边界 —— 全是这个血泪教训换来的。</p>`},

    {t:"text", h:"🔗 调用惯例：一份沉默的合同", html:`
      <p>A 调用 B 时，参数放哪、返回值放哪、谁负责清理栈 —— 这套规矩叫<b>调用惯例（calling convention）</b>。</p>
      <p>比如 x86-64 Linux 的规矩：<b>前 6 个整数参数放寄存器</b>（rdi, rsi, rdx, rcx, r8, r9），
      多的才放栈上；<b>返回值放 rax</b>。为什么用寄存器？因为快，不用碰内存。</p>
      <p>为什么要有统一的惯例？因为你的代码和别人的库（甚至不同语言写的）要能互相调用。
      <b>这份合同让 C 写的库能被 Python 调用、让不同编译器编的代码能链接在一起。</b>
      又是一个「接口/抽象」的例子 —— 只要大家守同一份合同，内部怎么实现都行。</p>`}
  ],
  quiz:[
    {id:"w7bq1", q:"函数调用为什么用「栈」这种结构，而不是队列或别的？",
     opts:["栈省内存","调用是「后调用的先返回」，正好是后进先出（LIFO），和栈完全匹配","栈更快","历史原因"],
     answer:1, ex:"A调B、B调C，返回顺序是 C→B→A，即最后压入的最先弹出。这就是后进先出，栈天然吻合这个模式。"},
    {id:"w7bq2", q:"一个栈帧里通常不包含以下哪项？",
     opts:["返回地址","局部变量","函数参数","堆上分配的数据"],
     answer:3, ex:"栈帧装返回地址、参数、局部变量、保存的寄存器。堆上的数据不在栈帧里——栈帧里最多有一个指向堆数据的指针。"},
    {id:"w7bq3", q:"无限递归最终会导致什么？",
     opts:["内存泄漏","栈溢出（Stack Overflow），因为帧不断压入撞穿栈边界","堆溢出","死循环但不崩溃"],
     answer:1, ex:"每次递归调用压一个新帧，无终止条件就无限压，栈空间有限（1~8MB），压穿边界即栈溢出崩溃。改循环或确保终止条件可解决。"},
    {id:"w7bq4", q:"缓冲区溢出攻击的核心原理是？",
     opts:["让程序内存耗尽","往栈上数组写入超长数据，覆盖掉同在栈上的返回地址，劫持程序流","修改堆数据","破解密码"],
     answer:1, ex:"返回地址就在栈上、紧邻局部数组。不检查长度地写数组，溢出部分覆盖返回地址，函数返回时就跳去执行攻击者的代码。canary、NX、ASLR 都是针对它的防御。"},
    {id:"w7bq5", q:"x86-64 上函数的前几个整数参数通常通过什么传递？",
     opts:["全部通过栈","通过寄存器（如 rdi/rsi/rdx…），多出来的才用栈","通过堆","通过全局变量"],
     answer:1, ex:"调用惯例规定前 6 个整数参数走寄存器（快，不碰内存），返回值放 rax。统一的惯例让不同语言/编译器产出的代码能互相调用。"}
  ]
};

WORLDS.push({id:"w7", num:"世界 7", icon:"🎭", name:"程序运行的舞台", sub:"内存布局、指针与调用栈", nodes:["w7a","w7b"], badge:"stack_walk"});
